import { Container, Typography } from "@mui/material";

function LearnerDashboard() {
  return (
    <Container>
      <Typography variant="h3">Learner Portal</Typography>
      <Typography variant="body1">Here, learners can view their results.</Typography>
    </Container>
  );
}

export default LearnerDashboard;
