import { Container, Typography } from "@mui/material";

function Dashboard() {
  return (
    <Container>
      <Typography variant="h3">Welcome to the Dashboard</Typography>
      <Typography variant="body1">
        This is where students/teachers can view results.
      </Typography>
    </Container>
  );
}

export default Dashboard;
