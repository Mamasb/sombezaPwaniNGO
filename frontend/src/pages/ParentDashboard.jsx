import { Container, Typography } from "@mui/material";

function ParentDashboard() {
  return (
    <Container>
      <Typography variant="h3">Parent Portal</Typography>
      <Typography variant="body1">View your child's results.</Typography>
    </Container>
  );
}

export default ParentDashboard;
